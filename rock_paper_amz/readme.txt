How to run:
java -jar rock_paper-0.0.1-SNAPSHOT.jar

Built by:
maven

Project written using:
STS(Eclipse)


please note, you need to comment out:
UserInterface applicationUI = new AdvertisingDecaratorConsole(new Console(new ConsoleInput()));
in Application.main to play simple game, otherwise it will come by default with advertising campaign ;)

to play simple game uncomment this:
//UserInterface applicationUI = new Console(new ConsoleInput());

good luck.

